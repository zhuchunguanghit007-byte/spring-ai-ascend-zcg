# agent-store Docker Compose 环境变量示例（两容器版）
# 用法：cd agent-store/deploy && cp .env.example .env && vi .env
#
# 本 Compose 仅启动 adapter + edp-agent。
# Versatile Mock、Redis、MCP Server 由同事独立部署，在此填写可达地址。

# ── 端口映射（宿主机端口，可按需修改）──
EDP_AGENT_PORT=8190
ADAPTER_PORT=8191

# ── LLM（edp-agent 必填）──
EDP_AGENT_MODEL_API_KEY=
EDP_AGENT_MODEL_PROVIDER=OpenAI
EDP_AGENT_MODEL_NAME=deepseek-v4-pro
EDP_AGENT_MODEL_BASE_URL=https://api.deepseek.com/v1

# ── 外部 Redis（edp-agent 启动必需，由同事提供）──
# 填同事 Redis 的 IP 或主机名；同机 Docker 容器可用 host.docker.internal
EDPA_REDIS_HOST=113.44.224.121
EDPA_REDIS_PORT=6379
EDPA_REDIS_PASSWORD=
EDPA_REDIS_DB=0

# ── 外部 Versatile Mock REST（adapter 使用，必填）──
# {workflow_id}、{conversation_id} 为运行时占位符，不要替换成具体 ID
# 同机 Mock 映射到宿主机 30001 时用 host.docker.internal；跨机填 IP
VERSATILE_URL=http://host.docker.internal:30001/v1/0/agent-manager/workflows/{workflow_id}/conversations/{conversation_id}
VERSATILE_WORKFLOW_ID=mock_workflow
VERSATILE_WORKSPACE_ID=10
VERSATILE_RESULT_NODE_TYPE=QA
VERSATILE_RESULT_NODE_NAME=GXZQAResponseNode

# ── edp-agent → adapter / 直连 Versatile（映射 application.yml edpa.agent.versatile.*）──
EDP_AGENT_VERSATILE_A2A_URL=http://adapter-versatile:8191/a2a
# 启动校验器要求合法 http URL，切勿留空；通常与 VERSATILE_URL 指向同一 Mock 地址
EDP_AGENT_VERSATILE_URL=http://host.docker.internal:30001/v1/0/agent-manager/workflows/{workflow_id}/conversations/{conversation_id}

# ── MCP Server（edp-agent call_mcp 使用，可选，由同事提供）──
# application.yml 读取 EDP_MCP_*；旧名 MCP_* 仍可作为别名（compose 会自动映射）
EDP_MCP_MASTER_URL=
EDP_MCP_STANDBY_URL=
EDP_MCP_ACCESS_TOKEN=
EDP_MCP_APP_NAME=f-mlp
